#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Created on Thu Mar  3 09:55:25 2022

@author: vivian

list comprehension:

    old_list = [1,2,3]
    new _list = [x for x in old_list]
                 
same thing as:
    new list = []
    for x in old_list:
        new_list.append(x)

check if given element is in a given list:
    an_element in a_list:
        
    or not in a list:
    
    an_element not in a_list
    
e.g. 3 in [1,3,5] should return trrue
5 not in [1,3,5] --> false

list.index(item_to_check)
- ".index(item_to_check"
- look up the index of item_to_check in list

list.insert(index, item_to_insert)
- .insert(index, item_to_insert)
- add element to any position 

a = [1,3,5,7,9]
5 in a
- true

6 in a
- false

8 not in a
- true

a.index(7)
3 - position #

a.insert(position, element)
=> a.insert(0,100)

---> a = [100, 1, 3, 5, 7, 9]

csv = spread sheet








"""

